// createElement(), appendChild(), append(), prepend(), insertBefore(), removeChild(), remove()

/* insertAdjacentElement(
    'beforebegin'
    'afterend'
    'beforeend'
    'afterend',
    element
) */
